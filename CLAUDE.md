# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Stack

TanStack Start + Convex + Better Auth template.

- **Frontend**: TanStack Start (React SSR)
- **Backend**: Convex (serverless backend + real-time DB)
- **Auth**: Better Auth via Convex component

## Commands

```bash
pnpm dev        # runs frontend + backend
pnpm check      # format + lint + typecheck (auto-runs via hooks)
```
